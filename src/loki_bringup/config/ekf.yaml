ekf_filter:
  ros__parameters:
    use_sim_time: false
    frequency: 30.0
    two_d_mode: false
    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom

    # /imu/data_raw → madgwick → /imu/data
    imu0: /imu/data
    imu0_config: [false, false, false,
                  true,  true,  true,
                  false, false, false,
                  true,  true,  true,
                  false, false, false]
    imu0_remove_gravitational_acceleration: true
    imu0_noise_in_diagonal_only: true

    twist0: /dvl/twist_stamped
    twist0_config: [false, false, false,
                    false, false, false,
                    true,  true,  true,
                    false, false, false,
                    false, false, false]
                    
    # uncomment for pool test
    # pose0: /depth/pose
    # pose0_config: [false, false, true,    #depth
    #                false, false, false,
    #                false, false, false,
    #                false, false, false,
    #                false, false, false]
    # pose0_differential: false
    # pose0_relative: false